#include<stdio.h>
int a;
printf("Enter the year");
scanf("%d",&a);
if(a%d==0)
{
if(a%100==0)
{
if(a%400==0)
printf("%d is leap year");
else
printf("%d is not a leap year");
}
else
printf("%d is a leap year");
return (0);
}
